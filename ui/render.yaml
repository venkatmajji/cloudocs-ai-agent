services:
  - type: web
    name: cloudocs-ui
    runtime: python
    buildCommand: pip install -r ui/requirements.txt
    startCommand: streamlit run ui/app.py --server.port 10000 --server.address 0.0.0.0
    plan: free
